name: Validate
on:
  pull_request: {}
  push:
    branches: [main]
jobs:
  manifests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: python3 scripts/build_index.py --check
      - run: python3 scripts/build_index.py --out dist
