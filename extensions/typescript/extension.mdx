---
title: TypeScript
tagline: TypeScript and JavaScript for Phantom — highlighting, diagnostics, completion and navigation.
version: 1.0.7
author:
  name: Isac Petinate
  url: https://github.com/ipetinate
license: MIT
created: 2026-09-06
updated: 2026-09-06
icon: media/icon.png
tags: [typescript, javascript, jsx, language, lsp]
screenshots:
  - media/editor.png
---

TypeScript and JavaScript support for the Phantom editor. The extension teaches Phantom the four language ids the ecosystem uses — `typescript`, `typescriptreact`, `javascript`, `javascriptreact` — with their file types, comment markers and TextMate grammars, and connects **typescript-language-server**, the wrapper that drives `tsserver` over the Language Server Protocol. Formatting comes from **Prettier**, which Phantom resolves from the project itself; the extension does not contribute a formatter, and the section below explains why.

Highlighting works the moment the extension is installed. The server is optional.

## Highlights

<Showcase>
  <Features>
    <Feature title="Syntax highlighting" icon="brush">
      Keywords, template literals, decorators, object keys, numbers with `_` separators, capitalized types and the lowercase primitives `string`, `number`, `boolean`, `never`, `unknown`. Calls are painted before `(` and before a type argument, so `useState<Foo>()` reads as a call.
    </Feature>
    <Feature title="Language intelligence" icon="search">
      Diagnostics, hover, completion, go to definition, find references, rename and code actions from `tsserver`. Inlay hints for parameter names, variable types and return types are available through the server's own settings.
    </Feature>
    <Feature title="The version the project pins" icon="package">
      The wrapper drives the `tsserver.js` the workspace itself installed, so the editor type-checks with the same TypeScript the build uses — 5, 6 or 7. It also hosts tsserver plugins, which is how the Vue extension serves a single-file component.
    </Feature>
  </Features>
  <Screenshot src="media/editor.png" alt="A TypeScript file open in Phantom with highlighted keywords, types and strings, and one diagnostic underlined in red" caption="Highlighting and a diagnostic in the Phantom editor" />
</Showcase>

## Requirements

Phantom does not bundle the tools. Install the ones you want; the extension detects them on your `PATH`.

<Requirement command="typescript-language-server" install="brew install typescript-language-server" url="https://github.com/typescript-language-server/typescript-language-server">
The wrapper around `tsserver`: diagnostics, hover, completion, definitions, references, rename and code actions. It needs a TypeScript to drive — the project's own, or a global one. Without it the highlighter still works.
</Requirement>

<Requirement command="prettier" install="brew install prettier" url="https://prettier.io/docs/">
The formatter. Phantom runs the project's own `node_modules/.bin/prettier` when the project declares Prettier, and a Prettier from your `PATH` only on ⇧⌘F. Without either, Format does nothing for these files.
</Requirement>

## Getting started

<Steps>
  <Step title="Install the extension">
    In Phantom, open the Extensions pane in the sidebar (or Settings → Extensions) and press **Install** next to TypeScript.
  </Step>
  <Step title="Install the server">
    Run the Homebrew command above, or `npm install -g typescript-language-server typescript`. Phantom picks it up without a restart.
  </Step>
  <Step title="Open a TypeScript file">
    The first time a project needs the server, Phantom asks before starting it and remembers your answer for that extension.
  </Step>
  <Step title="Format">
    Press <Kbd>⌘</Kbd> <Kbd>⇧</Kbd> <Kbd>F</Kbd> to format the current file, or turn on Format on Save under Settings → Editor.
  </Step>
</Steps>

## What the extension adds

| Area | Contribution |
|---|---|
| File types | `.ts`, `.mts`, `.cts`, `.tsx`, `.js`, `.mjs`, `.cjs`, `.jsx` |
| Language ids | `typescript`, `typescriptreact`, `javascript`, `javascriptreact` |
| Language server | `typescript-language-server --stdio` |
| Editor | TextMate grammars `source.ts`, `source.tsx`, `source.js` and `source.js.jsx` (TypeScript-TmLanguage), `//` and `/* */` comment markers |
| Icon | TypeScript file icon in the sidebar and tabs |

Phantom 0.17.0 has no compiled-in table of languages and servers, so this extension is where TypeScript support comes from. Install it and the four languages exist; remove it and they do not.

## The other TypeScript extension

**TypeScript Native** offers the same four languages served by `tsc --lsp --stdio`, the language server inside TypeScript 7. It needs no wrapper and no `tsserver.js` — one binary is the whole toolchain.

Prefer this extension when the project pins TypeScript 5 or 6, when a tsserver plugin has to load, or when the repository holds `.vue`, `.svelte`, `.astro` or `.mdx` files. Prefer TypeScript Native when the only TypeScript on the machine is 7 and there is no `tsserver.js` for the wrapper to drive.

Both claim the same four language ids, so only one of them serves a file at a time. Installing both lists the second one as **Shadowed**; the Troubleshooting section says how to choose which wins.

Prettier is deliberately absent from the manifest. Phantom does not treat Prettier as one command for one language: `PrettierProject` walks up from the file looking for a config, an ignore file and a local binary, and hands the buffer to that Prettier with `--stdin-filepath`. Declaring a formatter here would put a global `prettier` from your `PATH` ahead of that resolution and run it on every save, including in repositories formatted by something else.

## Configuration

<Details summary="Compiler: tsconfig.json">

The server reads the project's own `tsconfig.json` — or `jsconfig.json` for a JavaScript project — from the directory of the file upwards. Phantom passes the project root as the workspace; nothing about the compiler is configured in the editor.

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "jsx": "react-jsx",
    "types": ["node"]
  },
  "include": ["src"]
}
```

A project that has no `tsconfig.json` is still served: the server falls back to inferred project settings, which is why a loose `.js` file gets completion at all.

</Details>

<Details summary="Server: initializationOptions">

Settings → Extensions has an **initializationOptions (JSON)** field per server. It is where the server's own options go, because `typescript-language-server` takes them there rather than from a file of its own.

```json
{
  "preferences": {
    "includeInlayParameterNameHints": "all",
    "includeInlayParameterNameHintsWhenArgumentMatchesName": false,
    "includeInlayFunctionParameterTypeHints": true,
    "includeInlayVariableTypeHints": true,
    "includeInlayPropertyDeclarationTypeHints": true,
    "includeInlayFunctionLikeReturnTypeHints": true,
    "includeInlayEnumMemberValueHints": true
  }
}
```

| Option | What it does |
|---|---|
| `tsserver.path` | An explicit path to the `tsserver.js` the wrapper should drive |
| `preferences.includeInlayParameterNameHints` | `none`, `literals` or `all` |
| `implementationsCodeLens` | Turns the implementations code lens on |
| `referencesCodeLens` | Turns the references code lens on |
| `plugins` | tsserver plugins to load, each with a `name` and a `location` |

</Details>

<Details summary="Formatter: .prettierrc">

Prettier reads the nearest `.prettierrc`, `.prettierrc.json`, `.prettierrc.yaml`, `prettier.config.js` or a `prettier` key in `package.json`, walking up from the file. It honours `overrides`, `extends`, `.editorconfig` and `.prettierignore` on the way.

```json
{
  "printWidth": 100,
  "tabWidth": 2,
  "semi": false,
  "singleQuote": true,
  "trailingComma": "all",
  "arrowParens": "avoid"
}
```

| Option | Default | Values |
|---|---|---|
| `printWidth` | `80` | integer |
| `tabWidth` | `2` | integer |
| `useTabs` | `false` | `true`, `false` |
| `semi` | `true` | `true`, `false` |
| `singleQuote` | `false` | `true`, `false` |
| `quoteProps` | `"as-needed"` | `as-needed`, `consistent`, `preserve` |
| `jsxSingleQuote` | `false` | `true`, `false` |
| `trailingComma` | `"all"` | `all`, `es5`, `none` |
| `bracketSpacing` | `true` | `true`, `false` |
| `bracketSameLine` | `false` | `true`, `false` |
| `arrowParens` | `"always"` | `always`, `avoid` |
| `endOfLine` | `"lf"` | `lf`, `crlf`, `cr`, `auto` |
| `singleAttributePerLine` | `false` | `true`, `false` |

</Details>

## Troubleshooting

<Details summary="The editor says typescript-language-server is not installed">

The bar above the file shows the install command. After installing, press **Check Again**; the binary must be on the `PATH` of your login shell.

</Details>

<Details summary="Could not find a valid TypeScript installation">

The wrapper says this, in full: `Could not find a valid TypeScript installation. Please ensure that the "typescript" dependency is installed in the workspace or that a valid tsserver.path is specified.` It drives a `tsserver.js` and could not find one — the workspace has no `node_modules/typescript`, and no global TypeScript 5 or 6 either. TypeScript 7 does not help: it ships no `tsserver.js` at all.

Three ways out, in the order most people want them:

1. Install the dependency the project should have: `npm install` in the workspace, or `npm install -D typescript`.
2. Point the wrapper at a `tsserver.js` yourself, with `tsserver.path` in the **initializationOptions (JSON)** field described above.
3. Use a server that needs no wrapper. Install the **TypeScript Native** extension, or set this server's **Command** to `tsc` and its **Arguments** to `--lsp --stdio` under Settings → Extensions, then press **Restart Server**. That server type-checks as TypeScript 7 whatever the project pins, so it is the wrong answer for a project pinned to 5 or 6.

</Details>

<Details summary="The contribution is listed as Shadowed">

Another extension already claims these languages — **TypeScript Native**, in almost every case, since it claims the same four language ids. A shadowed contribution is parsed and listed but not in effect, so that installing something can never quietly change a language you already had.

Settings → Extensions → the extension → the language section has a **Precedence** control, **Use This Instead of**, which puts that extension ahead for one language id until you turn it back. Each of `typescript`, `typescriptreact`, `javascript` and `javascriptreact` is a separate decision.

</Details>

<Details summary="Formatting did nothing">

Format on Save runs the project's own Prettier and only when the project declares one — a config file or `node_modules/.bin/prettier`. A Prettier from your `PATH` is used on ⇧⌘F alone, so that a stray global install cannot claim files in a repository formatted by another tool.

</Details>

<Callout kind="note" title="Permissions">
The tools run as your user, with access to your files and network, the same as if you started them from the terminal. Phantom asks before it runs a program from an extension for the first time, and asks again if the extension's manifest or the program's path changes. You can review or change the answer under Settings → Extensions.
</Callout>

## Changelog

**1.0.7** — Says that TypeScript Native exists, when to prefer it, and what to do about `Could not find a valid TypeScript installation`. Corrects three statements the removal of Phantom's compiled-in server table made false: the app no longer picks between two TypeScript servers, no longer shadows this extension with a built-in definition, and no longer has a Language Servers pane.

**1.0.6** — Ships the TextMate grammars `source.ts`, `source.tsx`, `source.js` and `source.js.jsx`, one per language id, in place of the manifest's token patterns and keyword lists. Colouring now needs Phantom 0.17.0.

**1.0.5** — Published under Isac Petinate.

**1.0.4** — Credits Isac Petinate, who packaged it, rather than the app.

**1.0.3** — The file explorer draws the same artwork the store shows.

**1.0.2** — Wears the artwork Phantom draws for it.

**1.0.1** — Uses the icon the app's own Symbols theme draws for the language.

**1.0.0** — Initial release: language definitions for the four TypeScript and JavaScript language ids, and typescript-language-server wiring with install and uninstall commands.

## License and credits

The extension is released under the MIT License. [typescript-language-server](https://github.com/typescript-language-server/typescript-language-server) is licensed under the Apache License 2.0, with the parts copied from [Visual Studio Code](https://github.com/microsoft/vscode) under the MIT License. [Prettier](https://github.com/prettier/prettier) is © James Long and contributors (MIT). TypeScript is © Microsoft (Apache-2.0); this extension is not affiliated with Microsoft. The grammars are [TypeScript-TmLanguage](https://github.com/microsoft/TypeScript-TmLanguage) by Microsoft (MIT), in the JSON form Visual Studio Code ships.
